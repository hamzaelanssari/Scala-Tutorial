object conditions {

  def main(args: Array[String]): Unit ={
    // taking a variable
    val a: Int = 610
    // val a: Int = 60
    // val a: Int = 61


    // if condition to check
    // for even number
    if(a % 2 == 0)
    {
      if(a > 500){
      println("Even Number + Number > 500")}
      else {
        println("Even Number + Number < 500")
      }
    }
    else
    {
      // This statement will execute
      println("Sudo Placement")
    }
  }
}
